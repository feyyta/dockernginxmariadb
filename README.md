# dockernginxmariadb
a full web infrastructure with docker-compose: NGINX (TLS-only entry point), WordPress on php-fpm and MariaDB, each built from scratch on debian:bookworm, with Docker secrets, named volumes and an isolated bridge network.
